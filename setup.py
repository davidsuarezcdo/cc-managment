from setuptools import setup

setup(
    name='cc-management',
    author='David Suarez',
    version='0.1',
    install_requires=[
        'PyGithub',
        'python-dotenv',
        'python-decouple',
    ],
)
